services:
  - type: web
    name: boxing-judge
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: GRADIO_SERVER_NAME
        value: "0.0.0.0"
      - key: GRADIO_SERVER_PORT
        value: "7860"
    healthCheckPath: /ping